# Текущее состояние проекта

Этот проект объединяет основную 3D-сцену на `Three.js` и Python-WebSocket-сервер, который считает орбиту, ориентацию, телеметрию и оценки угловой скорости спутника.

Главная идея такая:

- визуальная сцена остаётся красивой и кинематографичной;
- Python отвечает за физику и телеметрию;
- фронтенд только отображает состояние, фокус, графики и вспомогательные оси;
- обмен между частями идёт через WebSocket и синхронизируется по времени.

## Что реализовано в проекте

### Основная сцена

- Солнце находится в центре.
- Земля движется по орбите вокруг Солнца.
- Сохраняются визуальные эффекты:
  - Bloom
  - DOF
  - звёздный фон
  - атмосфера Земли
  - облака и ночная сторона Земли
  - плавная камера и фокус на объект
- Из объектов сцене оставлены:
  - Солнце
  - Земля
  - спутник CubeSat

### Спутник

- Спутник отображается как CubeSat с пропорцией `3:1:1`.
- Есть цветные оси `+X`, `+Y`, `+Z`.
- Есть отдельные расчётные оси для сравнения с истинной ориентацией.
- Спутник можно фокусировать клавишей `3`.
- Линия орбиты спутника отображается отдельно, как у Земли.

### Управление

- `1` - фокус на Солнце
- `2` - фокус на Земле
- `3` - фокус на спутнике
- `Esc` - снять фокус
- `Space` - пауза
- `[` / `-` - замедлить время
- `]` / `=` - ускорить время

### Телеметрия

- Телеметрия встроена в основную страницу как скрывающееся меню.
- Есть графики:
  - орбитальных параметров
  - истинной угловой скорости
  - оценённой угловой скорости
  - ошибки по угловой скорости
  - оценённой ориентации
  - ошибки ориентации
  - магнитометра
  - солнечных панелей
- Есть переключение метода оценки:
  - `boresight`
  - `panels`
  - `ekf`
- Есть ручные поля для задания:
  - `omegaX/omegaY/omegaZ`
  - `altitudeKm`
  - `inclinationDeg`
  - `raanDeg`
  - `trueAnomalyDeg`
- Есть экспорт телеметрии в текстовый файл.

### Python-сервер

Файл `scripts/satellite_ws_server.py`:

- поднимает асинхронный `asyncio` WebSocket-сервер;
- считает круговую орбиту спутника;
- интегрирует кватернион ориентации методом RK4;
- вычисляет магнитное поле по `IGRF-14`;
- вычисляет освещённость 6 граней CubeSat через `astropy.coordinates.get_sun`;
- рассчитывает оценки угловой скорости:
  - метод Бора по магнитометру
  - метод по солнечным панелям
  - расширенный фильтр Калмана
- отправляет клиенту JSON-состояние на каждом шаге.

## Что отправляет сервер

Каждое сообщение имеет тип:

```json
{
  "type": "state"
}
```

Основные поля:

```json
{
  "time": 0.1,
  "x": 6878.1,
  "y": 0.0,
  "z": 0.0,
  "vx": 0.0,
  "vy": 0.0,
  "vz": 0.0,
  "qx": 0.0,
  "qy": 0.0,
  "qz": 0.0,
  "qw": 1.0,
  "omegaX": 0.0,
  "omegaY": 0.0,
  "omegaZ": 0.5,
  "estimatorMethod": "ekf",
  "omegaEstimatedX": 0.0,
  "omegaEstimatedY": 0.0,
  "omegaEstimatedZ": 0.0,
  "omegaErrorX": 0.0,
  "omegaErrorY": 0.0,
  "omegaErrorZ": 0.0,
  "qEstimatedX": 0.0,
  "qEstimatedY": 0.0,
  "qEstimatedZ": 0.0,
  "qEstimatedW": 1.0,
  "qErrorAngleDeg": 0.0,
  "Bx": 0.0,
  "By": 0.0,
  "Bz": 0.0,
  "I_Xp": 0.0,
  "I_Xm": 0.0,
  "I_Yp": 0.0,
  "I_Ym": 0.0,
  "I_Zp": 0.0,
  "I_Zm": 0.0
}
```

Также передаются орбитальные параметры:

- `altitudeKm`
- `semiMajorAxisKm`
- `eccentricity`
- `inclinationDeg`
- `raanDeg`
- `argPeriapsisDeg`
- `meanAnomalyDeg`
- `trueAnomalyDeg`
- `trueAnomalyRad`
- `meanMotionRadPerS`
- `orbitalSpeedKmPerS`
- `radiusKm`
- `periodSeconds`

## Связь frontend и Python

### Синхронизация времени

Frontend периодически отправляет:

```json
{
  "type": "sync",
  "time": <float>,
  "timeScale": <float>,
  "paused": <bool>
}
```

Это позволяет:

- держать Python и визуализацию на одном времени;
- корректно пересчитывать положение и ориентацию;
- поддерживать ускорение/замедление;
- сохранять непрерывность симуляции.

### Переключение оценивателя

Клиент может отправить:

```json
{
  "type": "set_estimator",
  "method": "boresight" | "panels" | "ekf"
}
```

### Ручное управление моделью

Клиент может отправить:

```json
{
  "type": "set_body_rate",
  "omegaX": 0.0,
  "omegaY": 0.0,
  "omegaZ": 0.5
}
```

Или:

```json
{
  "type": "set_orbit",
  "altitudeKm": 500.0,
  "inclinationDeg": 45.0,
  "raanDeg": 0.0,
  "trueAnomalyDeg": 0.0
}
```

При изменении орбитальных параметров сервер пересобирает модель орбиты с текущего момента симуляции.

## Математика орбиты

Спутник движется по круговой орбите вокруг Земли.

### Исходные параметры

- высота: `500 km`
- наклонение: `45 deg`
- долгота восходящего узла: `0 deg`
- начальная истинная аномалия: `0 deg`
- эксцентриситет: `e = 0`

### Радиус орбиты

```text
r = R_earth + h
```

где:

- `R_earth = 6378.137 km`
- `h = 500 km`

### Среднее движение

```text
n = sqrt(mu / r^3)
```

где:

- `mu = 398600.4418 km^3/s^2`

### Истинная аномалия

Для круговой орбиты:

```text
u(t) = u0 + n * (t - t0)
```

### Переход в геоцентрическую систему

Используется стандартный поворот из орбитальной плоскости в геоцентрическую систему:

```text
x = r * (cos(Omega) * cos(u) - sin(Omega) * sin(u) * cos(i))
y = r * (sin(Omega) * cos(u) + cos(Omega) * sin(u) * cos(i))
z = r * (sin(u) * sin(i))
```

где:

- `Omega` - RAAN
- `i` - наклонение
- `u` - истинная аномалия

### Орбитальная скорость

Для круговой орбиты:

```text
v = sqrt(mu / r)
```

В проекте эта скорость передаётся как `vx`, `vy`, `vz` для визуального предсказания позиции спутника между пакетами.

### Возмущения орбиты

В текущей модели орбита не считается абсолютно идеальной. Добавлены упрощённые секулярные возмущения:

- `J2`, `J3`, `J4` дают медленную прецессию RAAN и аргумента перигея;
- атмосферное сопротивление даёт очень медленное уменьшение большой полуоси;
- давление солнечного света даёт слабый долгопериодический дрейф истинной аномалии.

Это не полноценный высокоточный пропагатор, а рабочая инженерная аппроксимация для визуализации и телеметрии.

## Математика ориентации

### Представление

Ориентация спутника хранится в кватернионе:

```text
q = [qx, qy, qz, qw]
```

где `qw` - скалярная часть.

### Уравнение движения кватерниона

Используется кинематическое уравнение:

```text
q_dot = 0.5 * q ⊗ [omega_x, omega_y, omega_z, 0]
```

где:

- `q` - кватернион ориентации
- `omega` - угловая скорость в системе тела
- `⊗` - произведение кватернионов

### Интегрирование

На сервере применяется `RK4` с шагом `0.1 s`.

Смысл:

```text
q(t + dt) = RK4(q(t), omega, dt)
```

Это даёт более устойчивую интеграцию, чем простой Эйлер, особенно при ускорении симуляции.

## Магнитометр

Для расчёта магнитного поля используется `IGRF-14`.

### Алгоритм

1. Геоцентрические координаты спутника переводятся в геодезические `lat/lon/alt`.
2. `pyIGRF14.igrf_value(lat, lon, alt, year)` возвращает магнитное поле в локальной системе `NED`.
3. Поле переводится в геоцентрическую систему.
4. Затем поле поворачивается в систему тела спутника через обратный кватернион ориентации.

### Выход

- `Bx`
- `By`
- `Bz`

Это компоненты магнитного поля в теле спутника.

### Шумы датчиков

Для приближения к реальным измерениям добавлены малые шумы:

- магнитометр получает аддитивный шум по осям;
- токи солнечных панелей получают относительный и абсолютный шум;
- шумы пока остаются простыми, без сложной корреляционной модели.

## Солнечные панели

Для солнечного вектора используется `astropy.coordinates.get_sun`.

### Расчёт освещённости граней

Для каждой грани CubeSat берётся нормаль:

- `+X`
- `-X`
- `+Y`
- `-Y`
- `+Z`
- `-Z`

Затем считается проекция направления на Солнце на нормаль грани:

```text
I_face = max(dot(sun_body, normal_face), 0) * solar_constant
```

где `solar_constant` масштабируется по расстоянию до Солнца.

### Выход

- `I_Xp`
- `I_Xm`
- `I_Yp`
- `I_Ym`
- `I_Zp`
- `I_Zm`

Шум пока не добавляется.

### Затмение Землёй

Перед расчётом освещённости выполняется тест на затмение:

```text
shadow_distance = |r_sat x u_sun|
```

где:

- `r_sat` - радиус-вектор спутника относительно Земли;
- `u_sun` - единичный вектор на Солнце;
- `x` - векторное произведение.

Если спутник находится на ночной стороне Земли и расстояние до линии Солнце-Земля меньше радиуса Земли, то солнечный поток на панели обнуляется:

```text
I_face = 0
```

Именно это отключает панели в тени планеты.

## Перевод координат в Three.js

Сервер отдаёт координаты в геоцентрической системе в километрах.

Во фронтенде используется перестановка осей:

```text
sceneVector = (x, z, y)
```

Затем применяется масштабирование:

```text
sceneOffset = sceneVector / 1_000_000
```

Это нужно, чтобы:

- Земля не была слишком маленькой;
- спутник оставался видимым рядом с Землёй;
- модель не проваливалась внутрь планеты;
- орбитальная геометрия сохранялась в аккуратной сценовой форме.

## Отображение спутника

Спутник в сцене имеет два набора осей:

1. Истинные оси корпуса.
2. Расчётные оси из оценённой ориентации.

Для расчётных осей используется относительная ориентация:

```text
q_relative = q_true^-1 * q_estimated
```

Именно это позволяет сравнивать расчётное и настоящее положение осей в одном визуальном корпусе, без лишнего сложения вращений.

## Оценка угловой скорости

### 1. Метод Бора по магнитометру

Используется изменение единичного вектора магнитного поля в теле спутника.

Пусть:

```text
b = B / |B|
```

Тогда производная:

```text
b_dot = -omega x b
```

Минимально-нормовая оценка:

```text
omega_perp = -b x b_dot
```

Это даёт оценку компоненты вращения, наблюдаемой по магнитометру.

### 2. Метод по солнечным панелям

Из шести токов панелей восстанавливается нормализованный вектор на Солнце в системе тела:

```text
s_body = normalize([I_Xp - I_Xm, I_Yp - I_Ym, I_Zp - I_Zm])
```

Затем по изменению этого вектора оценивается угловая скорость:

```text
s_dot = (s(t) - s(t - dt)) / dt
omega = -s x s_dot
```

Это физически согласованная схема для оценки вращения по солнечным датчикам.

### 3. Расширенный фильтр Калмана

EKF использует:

- магнитометр;
- солнечный вектор;
- построение измерительного кватерниона;
- обновление ориентации и угловой скорости.

#### Идея измерения ориентации

Из двух ненулевых наблюдаемых направлений строится ориентация тела относительно инерциальной/геоцентрической системы. Практически используется TRIAD-подобный подход:

- берётся нормализованный магнитный вектор;
- берётся нормализованный солнечный вектор;
- из пары векторов строится ортонормированный базис тела;
- аналогичный базис строится для опорной системы;
- затем из матрицы поворота восстанавливается кватернион.

#### Ошибка ориентации

```text
q_err = q_true^-1 ⊗ q_est
```

Ошибка в градусах:

```text
angle = 2 * acos(|dot(q_true, q_est)|)
```

#### Обновление ω

Оценка угловой скорости берётся из разности последовательных измерительных кватернионов:

```text
delta_q = q_prev^-1 ⊗ q_curr
```

Затем `delta_q` переводится в rotation vector и делится на `dt`.

Это позволяет показать:

- действительную `omega`
- расчётную `omega`
- ошибку по компонентам

## Телеметрия и экспорт

Из телеметрии можно экспортировать текстовый файл с:

- временем;
- методом оценки;
- орбитальными параметрами;
- истинной и оценённой угловой скоростью;
- ошибкой по `omega`;
- оценённым кватернионом;
- ошибкой ориентации;
- магнитометром;
- солнечными панелями.

## Основные файлы

- `src/main.ts` - основная сцена, камера, фокус, связь с WebSocket, размещение спутника.
- `src/render/satelliteFactory.ts` - CubeSat, оси, подписи, визуальные детали.
- `src/ui/telemetryPanel.ts` - встроенная панель телеметрии и экспорт.
- `src/render/telemetryCharts.ts` - canvas-графики телеметрии.
- `src/sim/satelliteSocket.ts` - клиент WebSocket и валидация пакетов.
- `scripts/satellite_ws_server.py` - Python-сервер симуляции.
- `scripts/dev_all.py` - единый запуск Python и frontend.

## Коротко

Сейчас проект - это не просто красивая космическая сцена, а связанная система:

- визуализация орбит;
- физическая симуляция спутника;
- телеметрия;
- оценка ориентации и угловой скорости;
- сравнение истинных и расчётных значений;
- экспорт данных для анализа.
